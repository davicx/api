/*
General Conversation — workflow (placeholder)

Symmetric with conversation/request/workflow.js.
General Conversation has no store/execute path today; reserved for future use
(e.g. memory, session context, safety gates).
*/

//Function A1: No-op workflow for General Conversation (not used yet)
async function run() {
    console.log('General Conversation workflow not being used yet');
    return null;
}

module.exports = { run };
