/*
Organization knowledge — company standards, repos, policies.
Empty object for now; expandable without schema changes.
*/

function getOrganizationKnowledgeContext() {
    return {};
}

module.exports = {
    getOrganizationKnowledgeContext
};
